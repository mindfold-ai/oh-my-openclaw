# Component Guidelines

> N/A — Oh My OpenClaw has no UI components.

---

## Overview

This project is a CLI toolkit + OpenClaw plugin. There are no React components,
no browser UI, and no DOM rendering.

If a future version adds a UI (e.g., a dashboard), revisit this file and document
the component patterns used.
