# Hook Guidelines

> N/A — Oh My OpenClaw has no React hooks.

---

## Overview

This project has no React runtime. The only "hook" concept is the OpenClaw plugin
`before_prompt_build` event hook, which is documented in
[Quality Guidelines](./quality-guidelines.md).
